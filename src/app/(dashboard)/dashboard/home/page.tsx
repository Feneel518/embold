import Banner from "@/components/dashboard/banner/Banner";

interface pageProps {}

const page = async ({}) => {
  return (
    <div>
      <Banner></Banner>
      {/* <Lists categories={null}></Lists> */}
    </div>
  );
};

export default page;
